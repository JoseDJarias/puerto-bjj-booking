# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  user: admin            # Existe en users.yml
  class_schedule: one    # Existe en class_schedules.yml
  status: 0
  changed_by: admin      # Existe en users.yml