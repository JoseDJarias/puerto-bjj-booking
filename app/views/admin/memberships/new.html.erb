<% content_for :page_title, t("admin.memberships.new.title") %>
<div class="mx-auto">
  <h1 class="text-2xl font-black text-slate-900 mb-8 italic"><%= t("admin.memberships.new.title") %></h1>
  <%= render "form", membership: @membership %>
</div>